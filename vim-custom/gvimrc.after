if has("gui")

  " colorscheme solarized
  colorscheme smyck
  set guifont=Droid\ Sans\ Mono\ for\ Powerline:h12
  set autochdir
  set guioptions-=T       " hiding macvim's toolbar

  " xiki
  let $XIKI_DIR = "/usr/local/rvm/gems/ruby-1.9.3-p194-perf/gems/xiki-0.6.5"
  source /usr/local/rvm/gems/ruby-1.9.3-p194-perf/gems/xiki-0.6.5/etc/vim/xiki.vim

endif

"dont load csapprox if we no gui support - silences an annoying warning
if !has("gui")
  let g:CSApprox_loaded = 1
endif
